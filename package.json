{
  "name": "cocos-es6-browserify",
  "version": "0.0.1",
  "scripts": {
    "setup": "npm install && cocos new cocos-app -l js -d .",
    "build": "gulp build",
    "start": "gulp"
  },

  "devDependencies": {
    "babel-core": "^5.8.20",
    "babelify": "^6.1.3",
    "browserify": "^11.0.1",
    "browserify-incremental": "^3.0.1",
    "gulp": "^3.9.0",
    "gulp-rename": "^1.2.2",
    "lodash": "^3.10.0"
  }
}
